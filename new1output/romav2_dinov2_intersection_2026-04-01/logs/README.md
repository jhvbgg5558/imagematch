# Logs

This directory stores command traces and execution logs for the locked
`DINOv2 coarse + RoMa v2` bundle.

Expected files after execution:

- `run_commands.log`: launcher-level command history
- any stdout or stderr captures saved by the execution environment

This directory is intentionally lightweight before the first full run.
