# docs_md Usage Notes

This directory stores the active Markdown knowledge base for the current task.

Rules:

- prefer these files over old chat memory
- do not treat `../old/` as current truth
- if the task definition, valid data assets, or evaluation basis changes, update the Markdown docs
