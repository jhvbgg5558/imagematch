# Logs

This directory stores command traces and launcher logs for the
`DINOv2 coarse + RoMa v2 inlier-count-only` bundle.

Expected files after launch:

- `run_commands.log`
- environment-specific stdout and stderr logs if the bundle is launched in WSL
- any follow-up continuation logs written by post-process steps
